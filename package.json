{
  "name": "joyas-milinov",
  "private": true,
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "test": "node --test backend/test/server.test.js",
    "audit": "node scripts/audit-static.mjs",
    "verify": "npm run build && npm run audit && npm test"
  }
}
